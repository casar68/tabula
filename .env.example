# Storage directories
# DATA_DIR stores tabula.config.json (persistent app configuration).
# The database connection is configured via the setup wizard, not via env vars.
DATA_DIR=./backend/data
UPLOAD_DIR=./uploads

# Development
DEBUG=true
CORS_ORIGINS=["http://localhost:5173"]
